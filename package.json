{
  "name": "@ivanherula/clockify-mcp-server",
  "version": "0.1.0",
  "description": "MCP server for Clockify time tracking",
  "type": "module",
  "main": "./build/index.js",
  "bin": { "clockify-mcp-server": "./build/index.js" },
  "files": ["build", "README.md"],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "prepublishOnly": "npm run build"
  },
  "engines": { "node": ">=18.0.0" },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.27.1",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0"
  }
}
